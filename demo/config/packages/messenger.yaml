framework:
    messenger:
        failure_transport: failed

        transports:
            # Den 10: DSN z .env — doctrine://… (default) NEBO amqp://… (RabbitMQ)
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                retry_strategy:
                    max_retries: 3
                    multiplier: 2
            failed: 'doctrine://default?queue_name=failed'

        default_bus: command.bus

        buses:
            command.bus: []
            query.bus: []

        routing:
            Symfony\Component\Mailer\Messenger\SendEmailMessage: async
            Symfony\Component\Notifier\Message\ChatMessage: async
            Symfony\Component\Notifier\Message\SmsMessage: async

            App\Message\Command\CreateStudyTopicCommand: async
            App\Message\Event\StudyTopicCreatedMessage: async
            # Pulse pipeline — CreateWorkOrder zůstává sync (vrací ID)
            App\Message\Event\AdvanceWorkOrderMessage: async
