{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main style="max-width: 720px; margin: 2rem auto; font-family: system-ui, sans-serif; line-height: 1.5;">
        <h1>{{ title }}</h1>
        <p>První den — routing, controller, Twig. Projekt běží, struktura sedí.</p>

        <h2>Console příkazy (den 4)</h2>
        <pre style="background:#f4f4f4;padding:1rem;">php bin/console app:hello daker52
php bin/console app:hello daker52 --shout
php bin/console app:study:list --day=3
php bin/console app:study:seed</pre>

        <ul>
            <li><a href="{{ path('app_patterns_demo') }}">Design patterns demo (den 5)</a></li>
            <li><a href="{{ path('app_topics_index') }}">Study topics — Doctrine (den 3)</a></li>
            <li><a href="{{ path('app_services_demo') }}">Services &amp; DI demo (den 2)</a></li>
            <li><a href="{{ path('app_about') }}">About</a></li>
            <li><a href="{{ path('app_laravel_map') }}">Laravel → Symfony mapa</a></li>
            <li><a href="{{ path('app_study_topic', { topic: 'routing' }) }}">Study: routing</a></li>
            <li><a href="{{ path('app_study_topic', { topic: 'twig' }) }}">Study: twig</a></li>
            <li><a href="{{ path('app_api_ping') }}">API ping (JSON)</a></li>
            <li><a href="{{ path('app_api_version') }}">API version (JSON)</a></li>
        </ul>
    </main>
{% endblock %}
